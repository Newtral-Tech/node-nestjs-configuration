import { getConfigurationInjectionToken } from './get-configuration-injection-token';

/** Injection token to inject the whole configuration object */
export const CONFIGURATION = getConfigurationInjectionToken('configuration');
